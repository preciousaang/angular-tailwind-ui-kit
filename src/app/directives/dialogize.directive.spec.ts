import { DialogizeDirective } from './dialogize.directive';

describe('DialogizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogizeDirective();
    expect(directive).toBeTruthy();
  });
});
